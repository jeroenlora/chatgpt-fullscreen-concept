{
  "name": "chatgpt-fullscreen-concept",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "description": "Minimal reproduction of ChatGPT dropping acknowledged MCP Apps model-context updates.",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.22.0",
  "scripts": {
    "build": "node scripts/build-widget.js",
    "prestart": "pnpm build",
    "start": "node src/start.js",
    "pretest": "pnpm build",
    "test": "node --test",
    "check": "pnpm build && node --check src/widget-app.js && node --check src/widget.js && node --check src/server.js && node --check src/start.js && node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.5",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
